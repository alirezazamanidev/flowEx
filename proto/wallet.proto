syntax = "proto3";

package wallet;

message depositDto{
    string userId=1;
    string amount=2;
}
message depositResponse{
    string message=1;
    string gatewayUrl=2;
}
service WalletService {
    rpc deposit(depositDto) returns (depositResponse);
}